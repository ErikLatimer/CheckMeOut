import { CanvasDriverDirective } from './canvas-driver.directive';

describe('CanvasDriverDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasDriverDirective();
    expect(directive).toBeTruthy();
  });
});
